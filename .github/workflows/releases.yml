on:
  workflow_run:
    workflows: [ "Build and Test" ]
    types:
      - completed

name: Create Release

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
